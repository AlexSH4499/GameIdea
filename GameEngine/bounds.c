#include "bounds.h"

void createBounds(mvector* u, mvector* v,float radius)
{
    u = scaleVector(u, radius);//Up vector
    v = scaleVector(v, radius);

    //set coordinates on plane
    //vertex add(u,v)
}
